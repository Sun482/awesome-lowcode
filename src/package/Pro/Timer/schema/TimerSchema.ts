import { componentType } from "@/constants/componentType";
import type { ComponentSchema } from "@/package/common";

export const TimerSchema: ComponentSchema = {
  type: componentType.Pro,
  name: "Timer",
  alia: "定时器"
};
