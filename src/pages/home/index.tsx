import { PageContainer } from "@ant-design/pro-layout";

export default function (props) {
  console.log(props);
  return (
    <PageContainer style={{ height: "calc(100vh - 48px)" }}>home</PageContainer>
  );
}
